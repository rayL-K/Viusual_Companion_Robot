{
  "name": "visual-companion-cloudflare-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types --include-runtime false"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260702.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.103.0"
  }
}

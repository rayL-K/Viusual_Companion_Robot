# 仅作字段清单；不要在仓库副本中填写。
# 复制到 /etc/anima/anima.secret.env，root:root 0600。anima.service 通过
# systemd LoadCredential 把它只读投递到 /run/credentials；不要 source、打印或备份明文。
ANIMA_LLM_API_KEY=
ANIMA_ADMISSION_SECRET=
ANIMA_TELEMETRY_HMAC_KEY=
ANIMA_TURNSTILE_SECRET=
ANIMA_LOGIN_FERNET_KEY=

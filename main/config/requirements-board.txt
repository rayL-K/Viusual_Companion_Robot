# ELF2 (RK3588) 板端 Python 依赖
# ARM64 Ubuntu 22.04+, Python 3.11, CPU + NPU 推理

# ── 核心运行时 ──
PyYAML>=6.0,<7.0
numpy>=1.24,<2.0

# ── 摄像头采集 ──
opencv-python-headless>=4.9
Pillow>=10.0

# ── 视觉感知：YOLO NPU 推理 ──
# rknn-toolkit2 / rknpu2 — 从 RK3588  SDK 安装，非 pip

# ── 视觉感知：开发机 CPU 降级 ──
# onnxruntime>=1.18   # pip install onnxruntime

# ── LLM 对话：Qwen2.5-1.5B 本地推理 ──
# llama-cpp-python    # pip install llama-cpp-python (ARM 需编译)

# ── 语音识别 + 合成 + VAD：sherpa-onnx ──
sherpa-onnx>=1.10     # ARM64 预编译包

# ── 语音合成 (TTS)：VoxCPM2（备选） ──
# voxcpm>=2.0.3       # 按需安装，2B 模型需评估 RK3588 CPU 性能
# soundfile>=0.12

# ── 语音打断 (VAD) ──
webrtcvad>=2.0        # 轻量 C 扩展，替代 Silero VAD + PyTorch

# ── Web 服务 ──
# 前端 Vite build → 静态文件由 Python http.server 托管
